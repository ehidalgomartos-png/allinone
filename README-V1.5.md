# Instant Admirers V1.5 — PWA

La aplicación puede instalarse desde el navegador y ejecutarse en modo `standalone`. El Service Worker solo cachea recursos públicos del shell; no cachea respuestas autenticadas de API ni sockets.

Estado esperado en `/api/health`: `version: 1.5.0`.
